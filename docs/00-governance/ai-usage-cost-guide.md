# AI usage and estimated-cost guide

Actual token counts are stored only when returned by the provider. Missing
usage remains unavailable and is never fabricated. Pricing rows are typed,
owner-scoped, effective-dated, and empty by default. A cost appears only when a
matching operator-maintained rate exists and is always labelled estimated.
Usage summaries are bounded and owner-scoped; they are not provider invoices.
