# ADR 0039: Owner-scoped Campaign selectors

Campaign selectors use bounded lookup endpoints with stable sorting, safe labels, owner filters,
disabled-state reasons, and exact approved Artifact versions. Internal identifiers remain submitted
values, not the primary displayed experience.
