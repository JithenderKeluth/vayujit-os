# VAYUJIT OS 0.1.0-rc.1

## Scope

The first Windows local-MVP release packages the secure Electron shell and Angular renderer and
documents the supported local FastAPI/PostgreSQL runtime.

## Included capabilities

Owner authentication, brands, products, deterministic AI content, approvals, mock/WordPress/
Shopify publishing flows, campaigns, durable scheduling and workers, Recovery, rescheduling,
catch-up, Operations, backups, maintenance mode, accessibility lint, and the local performance
baseline.

## Supported platform and prerequisites

Windows 10/11 x64, PostgreSQL 17, and Python 3.12. PostgreSQL and Python are prerequisites and
are not bundled. Redis is not required.

## Installation and first run

Download `VAYUJIT-OS-0.1.0-rc.1-Setup.exe` and its `.sha256` file, verify the checksum, install as
the current user, install the packaged API runtime, start the API launcher, then launch VAYUJIT
OS. First launch presents owner setup; subsequent launches present login.

## Upgrade and backup

Close the application and run the newer per-user installer. The packaged API launcher creates and checksums a custom-format `pg_dump` backup under `%LOCALAPPDATA%\VAYUJIT OS\backups` before `alembic upgrade head`; any backup failure stops startup and leaves the database untouched.

## Security and data

Electron retains sandboxing, context isolation, disabled Node integration, web security, app-scheme
navigation, and denied permission requests. Connector credentials remain encrypted and are never
packaged. User data lives under `%LOCALAPPDATA%\VAYUJIT OS`; uninstall preserves it by default.

## Connector status and limitations

Mock connector acceptance is deterministic. WordPress and Shopify tests use controlled fakes; real
remote services are not contacted by the release validation. Automatic updates, cloud hosting,
Redis, multi-user authorization, and remote deletion are not included. Visual screen-reader and
contrast review remains a release-environment follow-up.

## Final acceptance status

- PASS: Docker Desktop 29.6.2 and PostgreSQL 17 development runtime; release prerequisites passed with the healthy documented container.
- PASS: migration chain 0001 through 0022, downgrade/upgrade cycle, and disposable migration database cleanup.
- PASS: backup create/list/verify/restore-plan for `20260808T064629Z-189e15c9`; metadata and SHA-256 verified.
- PASS: full test matrix, campaign suites, scheduler/workers, Angular, Electron, builds, lint, format, package scan, and production audit.
- PASS: disposable per-user install, packaged Electron launch, uninstall data preservation, and reinstall data reuse.
- PASS: System Doctor completed without hanging.
- PASS: positive packaged launcher backup-before-upgrade used official PostgreSQL 17.10 host client tools via `VAYUJIT_PG_DUMP_PATH`; `pg_dump`, `pg_restore`, and `psql` resolved and matched major version 17.
- PASS: representative marked disposable upgrade simulation from `20260810_0020` to `20260812_0022`, backup-failure protection, post-upgrade API/authentication, and optional restore verification.
- NO-GO for public distribution: installer is unsigned.
- GO for final internal acceptance; public distribution remains NO-GO because the installer is unsigned.

## Public Windows signing status

The internal/local Windows RC gate is GO. Public distribution is NO-GO because no approved code-signing
certificate or trusted signing-service configuration is available in the release environment. The
certificate store contains only self-signed test certificates; no certificate subject or publisher
identity is accepted for public release. The unsigned artifact checksum must not be reused after signing.

When signing is available, inject electron-builder signing inputs through secure release infrastructure,
apply a trusted timestamp, verify both Authenticode signatures and the trusted publisher chain, then
regenerate `VAYUJIT-OS-0.1.0-rc.1-Setup.exe.sha256`. SmartScreen reputation may remain low for a new
publisher even after signature validation succeeds. The packaged executable metadata also requires a
signed-build verification pass because the current unsigned executable reports Electron/GitHub metadata.

Current unsigned artifact: `release/VAYUJIT-OS-0.1.0-rc.1-Setup.exe`, 102946646 bytes, SHA-256 `bd411bf64e456dd40e079ccabf874d2cb69f4b5fdffb465efdfeb8d62d57004f`. This checksum must be regenerated after signing.
